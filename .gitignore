node_modules
.next
.next-dev
out
.DS_Store
*.local
next-env.d.ts
*.tsbuildinfo

# katalogun kendisi repoda tutulur (salt okunur ortamlarda tohum verisi olarak
# gerekli); yalnızca atomik yazmanın geçici dosyaları yok sayılır
data/store/*.tmp

# ortam dosyalari
.env
.env*.local
# canli sunucudaki gizli anahtarlar (Stripe, admin parolasi, DB parolasi)
.env.prod

# veritabani yedekleri sunucuda durur, repoda degil
backups/

# rclone yapilandirmasi ve SSH anahtari (uzak yedek hedefi)
deploy/backup-secrets/

# bun kullaniliyor: npm lockfile'i yanlislikla olusursa yok say
package-lock.json
yarn.lock
pnpm-lock.yaml
